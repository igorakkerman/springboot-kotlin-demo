# server:
#  ssl:
#    enabled: true
#    key-stora-type: PKCS12
#    key-store: path/to.p12
#    key-store-password:

spring:
#  datasource:
#    url: jdbc:h2:mem:test
  jpa:
    properties:
      hibernate:
#        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true

logging:
  level:
    de.igorakkerman.demo: DEBUG
#    org.hibernate: DEBUG
    org.hibernate.type: TRACE
#    org.springframework.transaction: TRACE